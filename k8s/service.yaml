apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer   # change to ClusterIP if you prefer using Ingress
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
